{
  "name": "winning-product",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "test": "vitest",
    "demo": "node scripts/seed-demo.js",
    "demo:reset": "node scripts/reset-demo.js",
    "dev": "npm run build && echo 'Extension built. Load dist/ folder in Chrome Extensions page.'",
    "clean": "rimraf dist/ && echo 'Build artifacts cleaned.'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "@google/genai": "^1.21.0",
    "@google/generative-ai": "^0.21.0",
    "@mistralai/mistralai": "^1.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.12",
    "@types/node": "^18.0.0",
    "esbuild": "^0.25.10",
    "jsdom": "^27.0.0",
    "typescript": "^5.9.2",
    "vitest": "^1.0.0"
  }
}
